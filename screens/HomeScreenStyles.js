import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "white",
    height: "100%",
  },

  uberIcon: {
    width: 100,
    height: 100,
    resizeMode: "contain",
  },

  contentContainer: {
    padding: 15,
  },
});
